# Backend

Local Flask API for the aviation tourism digital-human frontend.

Run:

```powershell
.\start-backend.cmd
```

OpenAvatarChat is expected at `http://127.0.0.1:8282` and can be started with:

```powershell
.\start-openavatar.cmd
```
